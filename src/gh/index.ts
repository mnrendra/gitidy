import version from './version'
import auth from './auth'
import repo from './repo'
import api from './api'

export {
  version,
  auth,
  repo,
  api
}

export default {
  version,
  auth,
  repo,
  api
}
