export type Options = {
  isForced?: boolean,
  skip?: boolean,
}

export type Result = {
  home: string,
  homeAlias: string,
  base: string,
  baseAlias: string,
  backup: string,
  backupAlias: string,
  repo: string,
  repoAlias: string,
  git: string,
  gitAlias: string,
  zip: string,
  zipAlias: string,
}
